name: Deploy MultiDocker
on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t guruiiii/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true guruiiii/react-test npm test

      - run: docker build -t guruiiii/multi-client ./client
      - run: docker build -t guruiiii/multi-nginx ./nginx
      - run: docker build -t guruiiii/multi-server ./server
      - run: docker build -t guruiiii/multi-worker ./worker

      - run: docker push guruiiii/multi-client
      - run: docker push guruiiii/multi-nginx
      - run: docker push guruiiii/multi-server
      - run: docker push guruiiii/multi-worker